{% extends 'admin.html.twig' %}

{% block title %}Edit Post{% endblock %}

{% block colclass %}col-md-9 mx-auto{% endblock %}

{% block pagetitle %}{% endblock %}

{% block body %}

    <div class="card">
        <div class="card-header">
            <h1 class="card-title">Editer l'article : {{ post.name }}</h1>
        </div>
        <div class="card-body">
            {{ include('admin/admin_posts/_form.html.twig', {'button_label': 'Enregistrer'}) }}

            <a href="{{ path('app_admin_posts_index') }}" class="btn btn-secondary">back to list</a>

            {{ include('admin/admin_posts/_delete_form.html.twig') }}
        </div>
    </div>

{% endblock %}
